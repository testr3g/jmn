{% if criteria is defined %}
	<div class="result_content">
		{{'message.search.noresult'|trans([],'JmnProductBundle')}}
	</div>
{%else%}
	<div class='numberfound'><b>{{'message.searchresult'|trans([],'JmnProductBundle')}}</b> : <span>{%if count is defined%}{{count}} {%else%}{{employees|length}}{%endif%}{{'message.found'|trans([],'JmnProductBundle')}}</span></div>
	{% include 'JmnProductBundle:Param:tbliste.html.twig' %}
{%endif%}
